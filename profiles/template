## generic_profile
## This is a profile for installing dotfiles for a specific platform

dirs=(
    # Insert directories here
    # Format:
    #
    #   "${script_dir}/path/to/dir,    ${config_dir}/symlink"
    #   "${script_dir}/path/to/dir,    ${config_dir}/symlink"
    #   "${script_dir}/path/to/dir,    ${config_dir}/symlink"
    #   ...
    #
)

files=(
    # Insert files here
    # Format:
    #
    #   "${script_dir}/path/to/file,    /path/to/symlink"
    #   "${script_dir}/path/to/file,    /path/to/symlink"
    #   "${script_dir}/path/to/file,    /path/to/symlink"
    #   ...
    #
)
